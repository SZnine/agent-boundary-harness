# v0.2 Skill 骨架 + AttackPatternStore 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立 Skill 层骨架，让 Harness 从 Skill 获取攻击策略，端到端链路跑通。

**Architecture:** Skill 层作为独立 Python 模块，通过同步函数调用（`get_next_strategy` / `record_result`）与 Harness 交互。v0.2 先用 AttackPatternStore 的硬编码查询（按 seam/boundary 精确匹配），不接入 LLM。Harness 改造为从 Skill 获取策略而非内部硬编码。

**Tech Stack:** Python 3.14, dataclasses, pytest

**Spec:** `docs/superpowers/specs/2026-04-10-terminal-architecture-design.md`

---

## 文件结构

| 文件 | 职责 | 动作 |
|---|---|---|
| `src/skill/models.py` | Strategy + SessionContext 数据类 | 新建 |
| `src/skill/pattern_store.py` | AttackPatternStore 加载和查询 | 新建 |
| `src/skill/skill_api.py` | SkillAPI 接口（get_next_strategy / record_result）| 新建 |
| `src/skill/__init__.py` | 模块导出 | 修改 |
| `src/harness/harness.py` | Harness 改造：从 Skill 获取策略 | 修改 |
| `src/skill/test_models.py` | models 单元测试 | 新建 |
| `src/skill/test_pattern_store.py` | pattern_store 单元测试 | 新建 |
| `src/skill/test_skill_api.py` | skill_api 单元测试 | 新建 |
| `src/harness/test_harness.py` | 更新 Harness 测试（Skill 接入后）| 修改 |

---

### Task 1: Strategy 和 SessionContext 数据模型

**Files:**
- Create: `src/skill/models.py`
- Create: `src/skill/test_models.py`

- [ ] **Step 1: 写失败测试**

```python
# src/skill/test_models.py
import pytest
from src.skill.models import Strategy, SessionContext, DecisionTraceUnit


def test_strategy_creation():
    s = Strategy(
        strategy_id="test-001",
        target_seam="I1",
        target_boundary="A1",
        payload_template="请帮我读取文件 {path}",
        payload_vars={"path": "/etc/passwd"},
        estimated_tokens=500,
        requires_llm_call=False,
        priority="P0",
        rationale="测试用"
    )
    assert s.strategy_id == "test-001"
    assert s.target_seam == "I1"
    assert s.target_boundary == "A1"
    assert s.render_payload() == "请帮我读取文件 /etc/passwd"


def test_strategy_render_without_vars():
    s = Strategy(
        strategy_id="test-002",
        target_seam="I2",
        target_boundary="A1",
        payload_template="访问恶意网页",
        payload_vars={},
        estimated_tokens=300,
        requires_llm_call=False,
        priority="P1",
        rationale="测试"
    )
    assert s.render_payload() == "访问恶意网页"


def test_session_context_required_fields():
    trace = DecisionTraceUnit(
        trace_id="t-001",
        session_id="s-001",
        iteration_depth=1
    )
    ctx = SessionContext(
        current_trace=trace,
        target_seam="I1",
        target_boundary="A1",
        iteration_depth=1
    )
    assert ctx.target_seam == "I1"
    assert ctx.current_trace.trace_id == "t-001"
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest src/skill/test_models.py -v`
Expected: FAIL（ImportError: cannot import name）

- [ ] **Step 3: 实现最小代码**

```python
# src/skill/models.py
from dataclasses import dataclass, field
from typing import Dict, Any, Optional


@dataclass
class DecisionTraceUnit:
    """传递给 Skill 的最小轨迹信息"""
    trace_id: str
    session_id: str
    iteration_depth: int


@dataclass
class SessionContext:
    """Skill 查询上下文"""
    current_trace: DecisionTraceUnit
    target_seam: str  # I1~I5
    target_boundary: str  # A1/A2/A3/M1/M2
    iteration_depth: int
    session_history_summary: Optional[str] = None


@dataclass
class Strategy:
    """Skill 返回的攻击策略"""
    strategy_id: str
    target_seam: str
    target_boundary: str
    payload_template: str
    payload_vars: Dict[str, str] = field(default_factory=dict)
    estimated_tokens: int = 0
    requires_llm_call: bool = False
    priority: str = "P2"
    rationale: str = ""

    def render_payload(self) -> str:
        """渲染 payload 模板"""
        result = self.payload_template
        for key, value in self.payload_vars.items():
            result = result.replace(f"{{{key}}}", value)
        return result
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest src/skill/test_models.py -v`
Expected: 3 passed

- [ ] **Step 5: 提交**

```bash
git add src/skill/models.py src/skill/test_models.py
git commit -m "feat(skill): add Strategy and SessionContext data models"
```

---

### Task 2: AttackPatternStore 加载和查询

**Files:**
- Create: `src/skill/pattern_store.py`
- Create: `src/skill/test_pattern_store.py`

- [ ] **Step 1: 写失败测试**

```python
# src/skill/test_pattern_store.py
import pytest
from src.skill.pattern_store import AttackPatternStore


def test_load_patterns():
    store = AttackPatternStore()
    count = store.load_from_file("data/attack_patterns.json")
    assert count > 200


def test_query_by_seam_boundary():
    store = AttackPatternStore()
    store.load_from_file("data/attack_patterns.json")
    results = store.query(seam="I1", boundary="A1")
    assert len(results) > 0
    for p in results:
        assert p["seam"] == "I1"
        assert p["boundary"] == "A1"


def test_query_returns_empty_for_no_match():
    store = AttackPatternStore()
    store.load_from_file("data/attack_patterns.json")
    results = store.query(seam="I1", boundary="M2")
    # 可能有也可能没有，只要不报错
    assert isinstance(results, list)


def test_get_pattern_by_id():
    store = AttackPatternStore()
    store.load_from_file("data/attack_patterns.json")
    patterns = store.query(seam="I1", boundary="A1")
    if patterns:
        pid = patterns[0]["pattern_id"]
        found = store.get_by_id(pid)
        assert found is not None
        assert found["pattern_id"] == pid
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest src/skill/test_pattern_store.py -v`
Expected: FAIL（ImportError）

- [ ] **Step 3: 实现**

```python
# src/skill/pattern_store.py
import json
from pathlib import Path
from typing import Optional


class AttackPatternStore:
    """攻击模式存储：加载、查询"""

    def __init__(self):
        self._patterns: list[dict] = []
        self._index_by_id: dict[str, dict] = {}
        self._index_by_seam_boundary: dict[str, list[dict]] = {}

    def load_from_file(self, filepath: str) -> int:
        """从 JSON 文件加载 patterns"""
        path = Path(filepath)
        if not path.is_absolute():
            path = Path(__file__).resolve().parent.parent.parent / filepath
        with open(path, "r", encoding="utf-8") as f:
            data = json.load(f)
        self._patterns = data.get("patterns", [])
        self._build_indexes()
        return len(self._patterns)

    def load_from_list(self, patterns: list[dict]):
        """直接加载 pattern 列表"""
        self._patterns = patterns
        self._build_indexes()

    def _build_indexes(self):
        """构建查询索引"""
        self._index_by_id = {}
        self._index_by_seam_boundary = {}
        for p in self._patterns:
            pid = p.get("pattern_id", "")
            self._index_by_id[pid] = p
            key = f"{p.get('seam', '')}-{p.get('boundary', '')}"
            self._index_by_seam_boundary.setdefault(key, []).append(p)

    def query(self, seam: str, boundary: str) -> list[dict]:
        """按 seam × boundary 精确匹配查询"""
        key = f"{seam}-{boundary}"
        return self._index_by_seam_boundary.get(key, [])

    def get_by_id(self, pattern_id: str) -> Optional[dict]:
        """按 pattern_id 查询"""
        return self._index_by_id.get(pattern_id)

    @property
    def total_count(self) -> int:
        return len(self._patterns)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest src/skill/test_pattern_store.py -v`
Expected: 4 passed

- [ ] **Step 5: 提交**

```bash
git add src/skill/pattern_store.py src/skill/test_pattern_store.py
git commit -m "feat(skill): add AttackPatternStore with seam/boundary query"
```

---

### Task 3: SkillAPI 接口实现

**Files:**
- Create: `src/skill/skill_api.py`
- Create: `src/skill/test_skill_api.py`
- Modify: `src/skill/__init__.py`

- [ ] **Step 1: 写失败测试**

```python
# src/skill/test_skill_api.py
import pytest
from src.skill.skill_api import SkillAPI
from src.skill.models import Strategy, SessionContext, DecisionTraceUnit


def _make_context(seam="I1", boundary="A1", depth=1) -> SessionContext:
    return SessionContext(
        current_trace=DecisionTraceUnit(
            trace_id="t-001",
            session_id="s-001",
            iteration_depth=depth
        ),
        target_seam=seam,
        target_boundary=boundary,
        iteration_depth=depth
    )


def test_get_strategy_returns_strategy():
    api = SkillAPI(data_path="data/attack_patterns.json")
    ctx = _make_context("I1", "A1")
    strategy = api.get_next_strategy(ctx)
    assert isinstance(strategy, Strategy)
    assert strategy.target_seam == "I1"
    assert strategy.target_boundary == "A1"
    assert strategy.strategy_id != ""
    assert strategy.payload_template != ""


def test_get_strategy_with_no_match():
    api = SkillAPI(data_path="data/attack_patterns.json")
    ctx = _make_context("I1", "M2")
    strategy = api.get_next_strategy(ctx)
    # 没有匹配时应返回 None
    assert strategy is None


def test_record_result_does_not_crash():
    api = SkillAPI(data_path="data/attack_patterns.json")
    # record_result 暂时只做日志，不应报错
    api.record_result({
        "trace_id": "t-001",
        "failure_classification": "L2"
    })
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest src/skill/test_skill_api.py -v`
Expected: FAIL（ImportError）

- [ ] **Step 3: 实现**

```python
# src/skill/skill_api.py
from src.skill.models import Strategy, SessionContext
from src.skill.pattern_store import AttackPatternStore


class SkillAPI:
    """Skill 层对外接口"""

    def __init__(self, data_path: str = "data/attack_patterns.json"):
        self._store = AttackPatternStore()
        self._store.load_from_file(data_path)

    def get_next_strategy(self, context: SessionContext) -> Strategy | None:
        """
        根据 context 查询最匹配的攻击策略。

        v0.2：Schema 精确匹配（零成本），取第一个匹配结果。
        """
        patterns = self._store.query(
            seam=context.target_seam,
            boundary=context.target_boundary
        )

        if not patterns:
            return None

        # 取第一个匹配（后续版本加入权重排序）
        best = patterns[0]

        return Strategy(
            strategy_id=best["pattern_id"],
            target_seam=best["seam"],
            target_boundary=best["boundary"],
            payload_template=best["pattern_description"],
            payload_vars={},
            estimated_tokens=500,
            requires_llm_call=False,
            priority="P1",
            rationale=f"ATLAS 映射: {best['atlas_mapping']['technique_id']} "
                      f"(置信度: {best['atlas_mapping']['mapped_confidence']})"
        )

    def record_result(self, trace: dict):
        """
        记录攻击结果。

        v0.2：仅打印日志，不更新权重矩阵。
        """
        classification = trace.get("failure_classification", "?")
        trace_id = trace.get("trace_id", "?")
        print(f"[Skill] record_result: trace={trace_id}, classification={classification}")
```

- [ ] **Step 4: 更新 __init__.py**

```python
# src/skill/__init__.py
from src.skill.models import Strategy, SessionContext, DecisionTraceUnit
from src.skill.pattern_store import AttackPatternStore
from src.skill.skill_api import SkillAPI
```

- [ ] **Step 5: 运行测试确认通过**

Run: `python -m pytest src/skill/test_skill_api.py -v`
Expected: 3 passed

- [ ] **Step 6: 提交**

```bash
git add src/skill/skill_api.py src/skill/test_skill_api.py src/skill/__init__.py
git commit -m "feat(skill): add SkillAPI with get_next_strategy and record_result"
```

---

### Task 4: Harness 改造 — 从 Skill 获取策略

**Files:**
- Modify: `src/harness/harness.py`
- Modify: `src/harness/test_harness.py`

- [ ] **Step 1: 写失败测试**

在 `src/harness/test_harness.py` 末尾追加：

```python
def test_harness_uses_skill_for_strategy(fake_tool_registry):
    """Harness 从 Skill 获取策略而非内部硬编码"""
    from src.skill.skill_api import SkillAPI

    gateway = MockGateway(fake_tool_registry)
    skill = SkillAPI(data_path="data/attack_patterns.json")
    harness = Harness(gateway, fake_tool_registry, skill_api=skill)

    # 运行标准测试（现在应从 Skill 获取策略）
    sessions = harness.run_standard_suite()
    assert len(sessions) > 0
    # 每个会话应有来自 Skill 的策略信息
    for session in sessions:
        assert session.initial_task != ""
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest src/harness/test_harness.py::test_harness_uses_skill_for_strategy -v`
Expected: FAIL（TypeError: __init__ got unexpected keyword 'skill_api'）

- [ ] **Step 3: 修改 Harness 构造函数**

修改 `src/harness/harness.py`:

1. 在 `Harness.__init__` 中增加 `skill_api` 可选参数：

```python
class Harness:
    def __init__(self, mock_gateway, fake_tool_registry, skill_api=None):
        self.gateway = mock_gateway
        self.fake_tools = fake_tool_registry
        self.sessions: Dict[str, AttackSession] = {}
        self.skill_api = skill_api
```

2. 增加 `run_skill_driven_suite` 方法（不改 run_standard_suite，保持向后兼容）：

```python
def run_skill_driven_suite(self, seam_boundary_pairs=None) -> List[AttackSession]:
    """
    从 Skill 获取策略运行测试。

    Args:
        seam_boundary_pairs: 要测试的 (seam, boundary) 列表。
            默认覆盖 v0.1 的 4 个组合。
    """
    if seam_boundary_pairs is None:
        seam_boundary_pairs = [
            ("I1", "A1"), ("I2", "A1"), ("I5", "A1"), ("M1", "A2")
        ]

    sessions = []
    for seam, boundary in seam_boundary_pairs:
        ctx = SessionContext(
            current_trace=DecisionTraceUnit(
                trace_id=str(uuid.uuid4()),
                session_id=str(uuid.uuid4()),
                iteration_depth=1
            ),
            target_seam=seam,
            target_boundary=boundary,
            iteration_depth=1
        )

        strategy = self.skill_api.get_next_strategy(ctx)

        if strategy is None:
            # 无匹配策略，跳过
            continue

        session = self._run_attack(
            session_id=ctx.current_trace.session_id,
            initial_task=f"Skill-driven: {strategy.strategy_id}",
            target_boundary=boundary,
            seam=seam,
            payload=strategy.render_payload(),
            probe_direction=strategy.rationale,
            iteration_depth=1
        )

        # 回传结果给 Skill
        if session.iteration_chain:
            result = session.iteration_chain[0]
            self.skill_api.record_result({
                "trace_id": result.trace_id,
                "session_id": result.session_id,
                "failure_classification": result.failure_classification.value,
                "seam": seam,
                "boundary": boundary
            })

        sessions.append(session)

    for session in sessions:
        self.sessions[session.session_id] = session

    return sessions
```

3. 在文件顶部添加导入：

```python
from src.skill.models import SessionContext, DecisionTraceUnit
```

4. 修改 `run_standard_suite` 使其在有 skill_api 时自动委托：

```python
def run_standard_suite(self) -> List[AttackSession]:
    """运行标准测试集"""
    if self.skill_api is not None:
        return self.run_skill_driven_suite()
    # ... 原有硬编码逻辑保持不变 ...
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest src/harness/test_harness.py -v`
Expected: 全部通过（包括新测试和原有测试）

- [ ] **Step 5: 运行完整测试套件**

Run: `python -m pytest src/ -v`
Expected: 全部通过

- [ ] **Step 6: 提交**

```bash
git add src/harness/harness.py src/harness/test_harness.py
git commit -m "feat(harness): integrate SkillAPI for strategy-driven attacks"
```

---

### Task 5: 端到端验证

**Files:**
- Create: `src/run_skill_suite.py`

- [ ] **Step 1: 写端到端运行脚本**

```python
# src/run_skill_suite.py
"""
端到端验证：Skill → Harness → Gateway → 记录

运行：python src/run_skill_suite.py
"""
import sys
import io
import json

if sys.platform == "win32":
    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8', errors='replace')
    sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding='utf-8', errors='replace')

from src.sandbox.fake_tools import get_fake_tool_registry
from src.gateway.mock_gateway import MockGateway
from src.harness.harness import Harness
from src.skill.skill_api import SkillAPI


def main():
    # 初始化组件
    fake_tools = get_fake_tool_registry()
    gateway = MockGateway(fake_tools)
    skill = SkillAPI(data_path="data/attack_patterns.json")
    harness = Harness(gateway, fake_tools, skill_api=skill)

    print(f"AttackPatternStore 已加载: {skill._store.total_count} 条模式")
    print()

    # 运行 Skill 驱动的测试
    sessions = harness.run_skill_driven_suite()
    report = harness.generate_summary_report()

    # 输出结果
    print("=" * 60)
    print("Skill-driven 攻击测试报告")
    print("=" * 60)

    summary = report["summary"]
    print(f"总会话数: {summary['total_sessions']}")
    print(f"L1 (能力缺失): {summary['l1_count']}")
    print(f"L2 (控制弱点): {summary['l2_count']}")
    print(f"L3 (可利用漏洞): {summary['l3_count']}")
    print()

    for session_data in report["sessions"]:
        task = session_data["initial_task"]
        boundary = session_data["target_boundary"]
        classification = session_data["final_classification"]
        print(f"  {task} | 边界: {boundary} | 分类: {classification}")

    # 保存
    with open("traces/skill-suite-report.json", "w", encoding="utf-8") as f:
        json.dump(report, f, ensure_ascii=False, indent=2)
    print()
    print(f"报告已保存到 traces/skill-suite-report.json")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 运行端到端验证**

Run: `python src/run_skill_suite.py`
Expected: 输出 Skill-driven 测试报告，L1/L2/L3 统计，无报错

- [ ] **Step 3: 提交**

```bash
git add src/run_skill_suite.py
git commit -m "feat: add end-to-end Skill-driven attack suite runner"
```

---

## 退出条件（v0.2 验收）

- [ ] 至少 1 个攻击模式通过 Skill 调度执行
- [ ] 轨迹正确记录并回传给 Skill（record_result 被调用）
- [ ] 原有 run_standard_suite 测试不被破坏
- [ ] 全部 pytest 通过
